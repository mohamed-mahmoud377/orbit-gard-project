$kind: http-request
url: "{{baseUrl}}/auth/promo-code?code=WELCOME500"
method: GET
queryParams:
  code: WELCOME500
scripts:
  - type: afterResponse
    code: |-
      pm.test('Promo code is valid', function () {
        pm.response.to.have.status(200);
        pm.expect(pm.response.json().status).to.eql('VALID');
      });
    language: text/javascript
order: 1000
