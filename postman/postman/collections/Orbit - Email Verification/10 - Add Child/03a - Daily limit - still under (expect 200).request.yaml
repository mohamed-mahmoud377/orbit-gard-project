$kind: http-request
description: Running total after request 02 + this one = 400.00, still under dailyLimit=500.00. Expect 200 success.
url: "{{baseUrl}}/api/v1/wallet/internal/transfer"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "receiverUsername": "{{receiverUsername}}",
      "amount": 100.00
    }
auth:
  type: bearer
  credentials:
    token: "{{childAccessToken}}"
order: 7000
