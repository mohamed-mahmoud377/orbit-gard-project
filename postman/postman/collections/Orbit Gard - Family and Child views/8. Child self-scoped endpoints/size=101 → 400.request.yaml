$kind: http-request
url: "{{baseUrl}}/child/transactions?size=101"
method: GET
queryParams:
  size: "101"
auth:
  type: bearer
  credentials:
    token: "{{childToken}}"
scripts:
  - type: afterResponse
    code: pm.test('400 Bad Request', () => pm.response.to.have.status(400));
    language: text/javascript
order: 7000
