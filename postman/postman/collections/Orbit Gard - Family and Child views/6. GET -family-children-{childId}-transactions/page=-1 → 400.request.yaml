$kind: http-request
url: "{{baseUrl}}/family/children/{{childAId}}/transactions?page=-1"
method: GET
queryParams:
  page: "-1"
scripts:
  - type: afterResponse
    code: pm.test('400 Bad Request', () => pm.response.to.have.status(400));
    language: text/javascript
order: 9000
