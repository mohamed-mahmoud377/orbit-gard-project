$kind: http-request
url: "{{baseUrl}}/family/children/{{childAId}}/transactions?size=0"
method: GET
queryParams:
  size: "0"
scripts:
  - type: afterResponse
    code: pm.test('400 Bad Request', () => pm.response.to.have.status(400));
    language: text/javascript
order: 7000
